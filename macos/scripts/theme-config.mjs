// Deliberately retained as a compatibility entrypoint for older launchers.
// Codex Skin never reads or modifies Codex config.toml.
console.log("theme-config is disabled: Codex Skin never reads or writes Codex config.toml.");
